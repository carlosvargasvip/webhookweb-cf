{
  "name": "webhookweb-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv WEBHOOKS",
    "deploy": "wrangler pages deploy public",
    "typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
